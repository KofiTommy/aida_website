<?php
declare(strict_types=1);
try { require_once __DIR__ . '/../app/bootstrap.php'; } catch (Throwable $e) { header('Location: ../setup.php'); exit; }
if (current_user()) { header('Location: index.php'); exit; }
$error='';
if ($_SERVER['REQUEST_METHOD'] === 'POST') { verify_csrf(); $email=filter_var(trim($_POST['email']??''),FILTER_VALIDATE_EMAIL); $password=$_POST['password']??''; $stmt=db()->prepare('SELECT * FROM users WHERE email=? AND is_active=1 LIMIT 1'); $stmt->execute([$email]); $user=$stmt->fetch(); if($user && password_verify($password,$user['password_hash'])) { login($user); db()->prepare('UPDATE users SET last_login_at=NOW() WHERE id=?')->execute([$user['id']]); audit('login','user',$user['id']); header('Location: index.php'); exit; } $error='Incorrect email address or password.'; }
?><!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>AIDA Admin</title><link rel="stylesheet" href="../assets/css/admin.css"></head><body class="admin-login"><main class="login-card"><a href="../index.php"><img src="../Logo.png" alt="AIDA"></a><p class="eyebrow"><span></span> AIDA CONTENT PORTAL</p><h1>Welcome back.</h1><p>Sign in to update the AIDA website.</p><?php if(isset($_GET['installed'])): ?><div class="admin-alert success">Your portal is ready. Sign in with the owner account you just created.</div><?php endif; ?><?php if($error): ?><div class="admin-alert error"><?= e($error) ?></div><?php endif; ?><form method="post" class="admin-form"><input type="hidden" name="csrf" value="<?= csrf() ?>"><label>Email address<input type="email" name="email" autocomplete="email" required></label><label>Password<input type="password" name="password" autocomplete="current-password" required></label><button class="admin-button" type="submit">Sign in →</button></form><a class="back-link" href="../index.php">← Return to website</a></main></body></html>
